---
title: "Concrete"
author:
  - Vasileios Papadopoulos
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## Dataset

```{r}
# install.packages("readxl")
# library("readxl")
concrete <- read_excel('Concrete_Data.xls')
attach(concrete)
head(concrete)
head(is.na.data.frame(concrete))
```
